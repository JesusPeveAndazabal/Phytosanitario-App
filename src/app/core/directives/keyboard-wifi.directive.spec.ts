import { KeyboardWifiDirective } from './keyboard-wifi.directive';

describe('KeyboardWifiDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardWifiDirective();
    expect(directive).toBeTruthy();
  });
});
